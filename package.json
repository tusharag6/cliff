{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "NEXT_PUBLIC_APP_ENV='emulator' dotenv -- turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "dotenv-cli": "^7.4.1",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.2.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
